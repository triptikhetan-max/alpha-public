---
description: Ask an Alpha Schools academic question — routed through the Alpha Academic API. Answers always include what we know, who to contact, and where the doc is.
---

Use the ask-alpha-academic skill to answer this Alpha academic question by calling the Alpha Academic API:

$ARGUMENTS

Read the skill's workflow carefully — check env vars first, pick the right endpoint (/ask for general questions, /dri/<area> for pure DRI lookups, /documents/search for finding a specific file), then render the response in the three-pillar format.
